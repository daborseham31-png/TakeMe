{
  "name": "takeme-id-reader",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that reads TakeMe ID / driving license photos via Gemini. Keeps the Gemini API key out of the Expo app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
